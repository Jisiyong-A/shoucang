[package]
name = "kan-kan-shou-cang"
version = "0.1.0"
description = "Local desktop shell for 看看收藏"
authors = ["Patrick"]
edition = "2021"

[lib]
name = "kan_kan_shou_cang_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
